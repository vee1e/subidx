/// <reference types="svelte" />
/// <reference types="vite/client" />

interface ImportMetaEnv {
  /** Absolute URL of the subidx API when the dashboard is hosted elsewhere. */
  readonly VITE_API_BASE?: string
}
